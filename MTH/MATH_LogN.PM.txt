If (False)  // MATH_LogN [Teddy LINET 08/2002]  // ------------------------------------------------------  // ---------------   F R A N C A I S  ---------------  // ------------------------------------------------------  // Rappels mathématiques :  // Calcul du logarithme dans la base choisie  // 10 est choisi s'il n'y a qu'un seul parametre (devient log10)  // soit en base n : Log(X):=ln(X)/ln(n)  // ---------------------------------------------  // MATHERROR  //  0 -> Pas d'erreur  // -1 -> argument <=0  // -2 -> base <=0  //  1 -> pas de base => base 10  // ---------------------------------------------  // EXEMPLES  // MATH_LogN(1;10):=0  // MATH_LogN(100;10):=2  // ------------------------------------------------------  // ---------------   E N G L I S H   -----------------  // ------------------------------------------------------  // Math's summary :  // Logarithm's calculation in choosen base  // 10 is choosed if there is only one parameter  (becomes log10)  // so in base n : Log(X):=ln(X)/ln(n)  // ---------------------------------------------  // MATHERROR  //  0 -> No error  // -1 -> argument <=0  // -2 -> base <=0  //  1 -> no base => base 10  // ---------------------------------------------  // EXEMPLES  // MATH_LogN(1;10):=0  // MATH_LogN(100;10):=2  // ------------------------------------------------------  //  (translation : BE 3-2010)End if   //@SVN_Folder: MATH_/C_REAL($0;$Y_F;$1;$vX_F;$2;$vBase_F)C_LONGINT(MATHERROR)MATHERROR:=0$vX_F:=$1If (Count parameters>1)$vBase_F:=$2Else $vBase_F:=10MATHERROR:=1End if Case of : ($vX_F<=0)MATHERROR:=-1$Y_F:=0: ($vBase_F<=0)MATHERROR:=-2$Y_F:=0Else $Y_F:=Log($vX_F)/Log($vBase_F)End case $0:=$Y_F