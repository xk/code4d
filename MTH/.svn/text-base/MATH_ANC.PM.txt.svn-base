If (False)  //MATH_ANC   //Auteur : Marc Duc-Jacquet  //Date : 2006  // ------------------------------------------------------  // ---------------   F R A N C A I S  ---------------  // ------------------------------------------------------  // Calcule le taux i qu'l faut appliquer pour   // qu'un emprunt de montant C ,   // puisse être remboursé en N échéances d'un montant A  // ---------------------------------------------  // MATH_ANC (A; N; C) -> i  // $1: N (Numérique) nombre d'échéances  // $2: A (Numérique) montant de chacune des N échéances  // $3: C (Numérique) capital emprunté  // $0: i calculé  // ---------------------------------------------  // MATHERROR  //  0 -> Pas d'erreur  // -1 -> Paramètres hors bornes  // ------------------------------------------------------  // ---------------   E N G L I S H   -----------------  // ------------------------------------------------------  // This method computes rate i to apply so that a loan C could be reimbursed  //in N payments of amount A  // ---------------------------------------------  // MATH_ANC (A; N; C) -> i  // $1: N (Real)    number of payments  // $2: A (Real)    amount of each payment  // $3: C (Real)    borrowed capital    // $0:     (Real)    i computed  // ---------------------------------------------  // MATHERROR  //  0 -> No error  // -1 -> Parameters out of range  // ---------------------------------------------  //  (translation : BE 03-2010)End if   //@SVN_Folder: MATH_/C_LONGINT(MATHERROR)C_REAL($1;$2;$3;$0;$n)$epsilon:=0,000001$N:=$2$k:=$1/$3$i0:=$k$Bool:=True$niter:=0While ($Bool)  // ecart > epsilon •BE• parenthèse$niter:=$niter+1$i:=$k*(1-(1/((1+$i0)^($N))))$Bool:=(Abs($i-$i0)>$epsilon)$i0:=$iEnd while $0:=$i*100