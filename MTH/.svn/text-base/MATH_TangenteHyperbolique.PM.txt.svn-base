If (False)  // MATH_TangenteHyperbolique [Marc Duc-Jacquet 23/03/2007]  // ------------------------------------------------------  // ---------------   F R A N C A I S  ---------------  // ------------------------------------------------------   // Rappels mathématiques :  // th(x) =sh(x)/ch(x)= (exp(x) - exp(-x))/(exp(x)+exp(-x))  //  // ---------------------------------------------  // MATHERROR  // 0 -> Pas d'erreur  // ------------------------------------------------------  // ---------------   E N G L I S H   -----------------  // ------------------------------------------------------  // Math's summary :  // th(x) =sh(x)/ch(x)= (exp(x) - exp(-x))/(exp(x)+exp(-x))  //  // ---------------------------------------------  // MATHERROR  // 0 -> No error  // ---------------------------------------------  //  (translation : BE 3-2010)End if   //@SVN_Folder: MATH_/C_LONGINT(MATHERROR)MATHERROR:=0C_REAL($1)  //Nombre dont il faut calculer la tangente hyperboliqueC_REAL($P;$Q)C_REAL($0)  //résultat du calcul $P:=Exp($1)$Q:=Exp(-$1)$0:=($P-$Q)/($P+$Q)