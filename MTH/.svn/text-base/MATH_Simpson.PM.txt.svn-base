If (False)  // MATH_Simpson [Marc Duc-Jacquet 10/2002]  // ------------------------------------------------------  // ---------------   F R A N C A I S  ---------------  // ------------------------------------------------------  // ATTENTION : Nécessite MATH_Evaluation  // ---------------------------------------------  // Rappels mathématiques : F(x) est continue sur (a,b)  // La méthode d'intégration  de F(x) sur l'intervalle (a,b) consiste  // à remplacer F par le raccordement de P paraboles ( N=2*P)  //  chacune de ces paraboles interpolant la fonction sur 3 points consécutifs  // ---------------------------------------------  // MATH_Simpson(F;A;B;N) --> Réel  // F (texte) expression de la fonction F(x)  // A et B (réels avec A<B)  lintervalle d'intégration  // N(entier) : nombre de sous-intervalles: DOIT ETRE PAIR  // le résultat: est l'aire algèbrique  approchée (intégrale de A à B de F)    // ---------------------------------------------  // MATHERROR  //  0 -> Pas d'erreur  // -1 -> 4ème paramètre nul ou impair  // -2 -> Mauvais intervalle (A >= B)  // MATH_Simpson comme MATH_Trapèzes calcule donc une intégrale défnie  // mais MATH_Simpson donnera à priori un meilleur résultat que MATH_Trapèzes  // pour un même nombre d'évaluations de F(x)   // ------------------------------------------------------  // ---------------   E N G L I S H   -----------------  // ------------------------------------------------------  // WARNING : this method needs MATH_Evaluation  // ---------------------------------------------  // Math's summary : F(x) is continuous on (a,b)  // F(x) integration method on range (a,b) consists to replace F by P parabolas junction (N=2*P),  // each of those parabolas interpoling function on 3 consecutive points   // ---------------------------------------------  // MATH_Simpson(F;A;B;N) --> Réel  // F (text) function's expression F(x)  // A et B (reals with A<B)  integration range  // N(entier) : number of sub-ranges: MUST BE EVEN  // Result is approximate algebraic area (F integral from A to B)  // ---------------------------------------------  // MATHERROR  //  0 -> No error  // -1 -> 4th parameter nil or odd  // -2 -> bad range (A >= B)  // MATH_Simpson as MATH_Trapèzes computes a definite integral  // but MATH_Simpson gives a priori a better result than MATH_Trapèzes  // for the same number of evaluations of F(x)   // ------------------------------------------------------  //  (translation : BE 3-2010)End if   //@SVN_Folder: MATH_/C_LONGINT(MATHERROR)C_TEXT($1)C_REAL($0;$2;$3;$X0;$SI;$SP;$T;$H)C_LONGINT($4;$N;$i)MATHERROR:=0$0:=0Case of : ($4=0)MATHERROR:=-1: (($4%2)#0)MATHERROR:=-1: ($2>=$3)MATHERROR:=-2End case If (MATHERROR=0)$X0:=$2$N:=$4  //nombre sous intervalles$H:=($3-$2)/$4$SP:=0For ($i;2;$N-2;2)  //évaluation somme des valeurs aux abscisses d'indice pair$T:=$X0+($i*$H)$SP:=$SP+MATH_Evaluation ($1;$T)End for $SI:=0For ($i;1;$N-1;2)  //évaluation somme des valeurs aux abscisses d'indice impair$T:=$X0+($i*$H)$SI:=$SI+MATH_Evaluation ($1;$T)End for $S:=MATH_Evaluation ($1;$2)+MATH_Evaluation ($1;$3)  // F(A)+F(B)$S:=$S+(4*$SI)+(2*$SP)$0:=$S*$H/3End if 