If (False)  //MATH_ArcTangente2  //Auteur :   //Date :  // ------------------------------------------------------  // ---------------   F R A N C A I S  ---------------  // ------------------------------------------------------  //Cette fonction retourne l'arctangente du couple x-y.  //L'arctangente est l'angle entre l'axe des X et une ligne passant par l'origine et un point de coordonnées X;Y.  //L'angle est donné en radians entre -pi et +pi, excluant pi.  //Syntaxe  //MATH_ArcTangente2(x As Double, y As Double)  //x   est l'abscisse du point.  //y   est l'ordonnée du point.  //Remarques  //- un résultat positif représente une rotation dans le sens contraire des aiguilles d'une montre  //- MATH_ArcTangente2(x;y) ets équivalent à Arctan (y/x), sauf que x peut être égal à zéro.  //- si les deux paramètres sont égaux à zéro, ????  //- pour obtenir le résultat en degrés, multiplier le résultat par 180/pi  // ---------------------------------------------  // ATTENTION : cette méthode nécessite une version de 4D utilisant les constantes  // ------------------------------------------------------  // ---------------   E N G L I S H   -----------------  // ------------------------------------------------------  //This fuction returns the arctangent, or inverse tangent, of the specified x-and y-coordinates,  in all four quad.   //The arctangent is the angle from the x-axis to a line containing the origin(0, 0)and a point with coordinates(x_num, y_num).  //The angle is given in radians between-pi and pi, excluding-pi.  //  //Syntax  //MATH_ArcTangente2(x As Double, y As Double)  //x   is the x-coordinate of the point.  //y   is the y-coordinate of the point.  //  //Remarks  //- a positive result represents a counterclockwise angle from the x-axis; a negative result represents a clockwise angle.  //- ATAN2(a,b) equals ATAN(b/a), except that a can equal 0 in ATAN2.  //- if both x_num and y_num are 0, ATAN2 returns the#DIV/0! error value.  //- to express the arctangent in degrees, multiply the result by 180/Pi ()or use the DEGREES function.  // ---------------------------------------------  // WARNING : this method needs a 4D version using constants.  // ---------------------------------------------  //  (translation : BE 03-2010)End if   //@SVN_Folder: MATH_/C_REAL($1;$2;$0;$y;$x)  //$y , $x$y:=$1$x:=$2Case of : ($y>0) & ($x=0)$0:=Pi/2: ($y<0) & ($x=0)$0:=-Pi/2: ($x>0) & ($y>0)$0:=Arctan($y/$x): ($x<0) & ($y>0)$0:=Arctan($y/$x)+Pi: ($x>0) & ($y<0)$0:=Arctan($y/$x): ($x<0) & ($y<0)$0:=Arctan($y/$x)+PiElse End case 