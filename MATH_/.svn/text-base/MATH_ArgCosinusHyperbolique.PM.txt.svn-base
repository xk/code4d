If (False)  //MATH_ArgCosinusHyperbolique   //Auteur : Marc Duc-Jacquet  //Date : 23/03/2007  // ------------------------------------------------------  // ---------------   F R A N C A I S  ---------------   // ------------------------------------------------------  // Rappels mathématiques :  // Argch(x) =ln(x + (x^2-1)^0,5) défini pour x>=1  // C'est la fonction réciproque de la fonction ch`  //  Argch(ch(x)) = x   // MATHERROR   // 0 -> Pas d'erreur  // -1 -> Nombre est en dehors du domaine de définition [1,...]  // ------------------------------------------------------  // ---------------   E N G L I S H   -----------------  // ------------------------------------------------------  //Mathematical reminders  //Argch(x) =ln(x + (x^2-1)^0,5) defined for x>=1  //It is reverse function of ch (MATH_CosinusHyperbolique)  // Argch(ch(x)) = x   // MATHERROR  // 0 -> No error  //-1 -> Number out of range [1,...]  // ---------------------------------------------  //  (translation : BE 03-2010)End if   //@SVN_Folder: MATH_/C_LONGINT(MATHERROR)C_REAL($1)  //Nombre dont il faut calculer l'argument cosinus hyperboliqueC_REAL($0)  //résultat du calcul $0:=0  // Traitement des erreursCase of : (Abs($1)<1)MATHERROR:=-1Else MATHERROR:=0End case If (MATHERROR=0)$0:=Log($1+Square root($1*$1-1))End if 