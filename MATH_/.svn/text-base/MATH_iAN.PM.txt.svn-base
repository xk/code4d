If (False)  // MATH_iAN [Marc Duc-Jacquet, 2006]  // ------------------------------------------------------  // ---------------   F R A N C A I S  ---------------  // ------------------------------------------------------  // Calcul du montant du principal C d'un emprunt., donnant lieu  // à un remboursement périodique constant egal à A,  //  N fois, le taux de cet emprunt sur la période étant égal à i  // ---------------------------------------------  // MATH_iAN (i; A; N) -> C  // i (Real) taux d'intérêt par période  // N (Real) nombre total de périodes  // A (Real) montant du remboursement périodique  // C (Real) capital emprunté  // ---------------------------------------------   // MATHERROR  //  0 -> Pas d'erreur  // -1 -> Paramètres hors bornes  // ------------------------------------------------------  // ---------------   E N G L I S H   -----------------  // ------------------------------------------------------  // This method computes the principal of a borrow, giving rise to a periodic reimbursment with a constant amount A  //  that occurs N times, interest rate being i for a period  // ---------------------------------------------  // MATH_iAN (i; A; N) -> C  // i (Real)   interst rate by period  // N (Real) total number of periods  // A (Real)  periodic reimbursment amount  // C (Real)  borrowed capital    // ---------------------------------------------  // MATHERROR  //  0 -> No error  // -1 -> Parameters out of range  // ---------------------------------------------  //  (translation : BE 03-2010)End if   //@SVN_Folder: MATH_/C_LONGINT(MATHERROR)C_REAL($1;$2;$3)C_REAL($0)$1:=$1/100$0:=0MATHERROR:=-1Case of : (((1+$1)^$3)=0): ($1*(1-(1/((1+$1)^$3)))=0)Else MATHERROR:=0$0:=Round($2/$1*(1-(1/((1+$1)^$3)));2)End case 