If (False)  // MATH_CosinusHyperbolique [Marc Duc-Jacquet 23/03/2007]  // ------------------------------------------------------  // ---------------   F R A N C A I S  ---------------   // ------------------------------------------------------  // Rappels mathématiques :  // ch(x) = ( exp(x)+exp(-x))/2 défini pour tout réel x  //  // ---------------------------------------------  // MATHERROR  // 0 -> Pas d'erreur  // ------------------------------------------------------  // ---------------   E N G L I S H   -----------------  // ------------------------------------------------------  // Math's summary:  // ch(x) = ( exp(x)+exp(-x))/2 defined for all real x  //  // ---------------------------------------------  // MATHERROR  // 0 -> No error  //  (translation : BE 03-2010)End if   //@SVN_Folder: MATH_/C_LONGINT(MATHERROR)MATHERROR:=0C_REAL($1)  //Nombre dont il faut calculer le cosinus hyperboliqueC_REAL($0)  //résultat du calcul $0:=(Exp($1)+Exp(-$1))/2