If (False)  // MATH_iNC [Marc Duc-Jacquet, 2006]  // ------------------------------------------------------  // ---------------   F R A N C A I S  ---------------  // ------------------------------------------------------  // Pour un montant emprunté égal à C le remboursement étant périodique  et constant en N fois   // le taux d'intérêt rapporté à la période( année, semestre, mois, ou quinzaine) étant égal à i (0< i <1 )  // On calcule le montant A à rembourser à chacune des N échéances  // ---------------------------------------------  // MATH_iNC (i; N; C) -> A  // i (Numérique) taux d'intérêt relaté à la période  // N (Numérique) nombre de périodes (échéances)  // C (Numérique) capital emprunté  // A (Numérique) montant de chaque remboursement   // ---------------------------------------------  // MATHERROR  // 0 -> Pas d'erreur  // ------------------------------------------------------  // ---------------   E N G L I S H   -----------------  // ------------------------------------------------------  // For a borrowed capital C, reimbursment being periodic and constant with N occurences,  // interest rate by period (year, quarter, month) being i (0< i <1 )  // this method computes Amount A to reimburse at each of N due dates.  // ---------------------------------------------  // MATH_iNC (i; N; C) -> A  // i (Real)   interest rate realted to period  // N (Real) number of periods (due dates)  // C (Real) borrowed capital    // A (Real) amout of each reimbursment  // ---------------------------------------------  // MATHERROR  //  0 -> No error  // ---------------------------------------------  //  (translation : BE 03-2010)End if   //@SVN_Folder: MATH_/C_LONGINT(MATHERROR)C_REAL($1;$2;$3)C_REAL($0)$0:=0$1:=$1/100MATHERROR:=-1Case of : (((1+$1)^$2)=0): ((1-(1/((1+$1)^$2)))=0)Else MATHERROR:=0$0:=($3*$1)/(1-(1/((1+$1)^$2)))End case 